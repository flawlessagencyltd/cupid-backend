{
  "name": "cupid-replica-functions",
  "description": "Uncensored AI chat landing page backend",
  "version": "1.0.0",
  "main": "index.js",
  "scripts": {
    "start": "node server.js",
    "check": "node --check index.js && node --check tracking.js && node --check server.js && node --check db.js && node --check geo.js && node --check http.js",
    "test": "node --test test/*.test.js",
    "load:test": "node scripts/load-test.js"
  },
  "dependencies": {
    "express": "^4.19.0",
    "firebase-admin": "^12.0.0",
    "firebase-functions": "^5.0.0",
    "node-fetch": "^2.7.0",
    "pg": "^8.13.1"
  },
  "engines": {
    "node": "20"
  },
  "private": true
}
